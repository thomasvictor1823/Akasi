import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Akasi - Your Marketplace for Everything",
  description:
    "Discover amazing products from individual sellers and businesses. Shop electronics, fashion, home goods and more on Akasi marketplace.",
  keywords: "marketplace, online shopping, electronics, fashion, home goods, business stores",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
